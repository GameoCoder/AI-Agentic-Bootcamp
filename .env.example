# ── Ollama (required) ─────────────────────────────────────────────────────────
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2:0.5b

# ── Embedding/Transformer Models (optional – defaults set in config.py) ────────
# SENTENCE_TRANSFORMER_MODEL=sentence-transformers/LaBSE
# WORD_EMBEDDING_MODEL=ai4bharat/indic-bert
# MT5_MODEL=google/mt5-small
# XLMR_MODEL=xlm-roberta-base

# ── Training Hyperparameters (optional) ───────────────────────────────────────
# BATCH_SIZE=16
# LEARNING_RATE=2e-5
# NUM_TRAIN_EPOCHS=3
# MAX_SEQ_LENGTH=128
# SYNTHETIC_SENT_COUNT=500

# ── External APIs (optional) ──────────────────────────────────────────────────
# OPENWEATHER_API_KEY=your_key_here

# ── Server Config (optional) ──────────────────────────────────────────────────
# API_HOST=0.0.0.0
# API_PORT=8000
